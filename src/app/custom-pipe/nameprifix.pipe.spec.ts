import { NameprifixPipe } from './nameprifix.pipe';

describe('NameprifixPipe', () => {
  it('create an instance', () => {
    const pipe = new NameprifixPipe();
    expect(pipe).toBeTruthy();
  });
});
